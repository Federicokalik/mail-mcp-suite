# Published image, for example
# ghcr.io/federicokalik/mail-mcp-suite:2.1.0
# Keep mail-mcp-suite:local only when building from source with
# compose.build.yaml.
MAIL_MCP_IMAGE=mail-mcp-suite:local
MAIL_MCP_CONFIG_DIR=./local-config
MAIL_MCP_BIND_ADDRESS=127.0.0.1
