# Optional override for building the image locally instead of pulling the
# published one. All four services share a single image, so only one of them
# needs a build context.
#
#   docker compose -f compose.yaml -f compose.build.yaml build
#   docker compose -f compose.yaml -f compose.build.yaml up -d

services:
  reader:
    build:
      context: .
